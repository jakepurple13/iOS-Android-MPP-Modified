<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="CustomButton" parent="Widget.AppCompat.Button">
        <item name="android:background">@android:color/transparent</item>
        <!--<item name="android:textColor">@drawable/custom_button_selector</item>-->
        <!--<item name="android:enterFadeDuration">@android:integer/config_shortAnimTime</item>
        <item name="android:exitFadeDuration">@android:integer/config_shortAnimTime</item>-->
    </style>

</resources>
